spring:
  application:
    name: efeito-mulher

  datasource:
    url: jdbc:mysql://localhost:3306/efeito_mulher?useSSL=false&serverTimezone=UTC
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: Yanai291202

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect

  http:
    encoding:
      charset: UTF-8
      enabled: true
      force: true

server:
  port: 8080

jwt:
  secret: MinhaChaveJWTMuitoSecretaEGrande123!
  expiracao: 86400000